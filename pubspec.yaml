name: flutter_osm_web
description: A new Flutter project using OpenStreetMap.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 核心地圖套件 (OpenStreetMap)
  flutter_map: ^6.1.0
  # 用來處理經緯度的工具
  latlong2: ^0.9.0

flutter:
  uses-material-design: true
